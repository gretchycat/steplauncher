<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="84dp"
    android:layout_height="90dp"
    android:layout_margin="4dp"
    android:background="@drawable/bg_frosted_glass_tile_base"
    android:clickable="true"
    android:focusable="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="4dp">

        <!-- Real Application Graphic Icon -->
        <ImageView
            android:id="@+id/iv_vfs_icon"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:scaleType="fitCenter"
            android:visibility="gone" />

        <!-- Text / Emoji Icon Symbol Fallback -->
        <TextView
            android:id="@+id/tv_vfs_symbol"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="📁"
            android:textSize="36sp"
            android:includeFontPadding="false" />

        <!-- App / Folder Title -->
        <TextView
            android:id="@+id/tv_vfs_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:text="Folder"
            android:textColor="@color/text_primary"
            android:textSize="10sp"
            android:textStyle="bold"
            android:gravity="center"
            android:maxLines="1"
            android:ellipsize="end" />

    </LinearLayout>

    <!-- Top Left Multi-Select Checkbox Indicator -->
    <TextView
        android:id="@+id/tv_vfs_check"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_gravity="top|start"
        android:layout_margin="2dp"
        android:textColor="@color/cyan_accent"
        android:textSize="12sp"
        android:textStyle="bold"
        android:gravity="center"
        android:text="☐"
        android:visibility="gone" />

    <!-- Top Right Attention / Notification Badge -->
    <TextView
        android:id="@+id/tv_vfs_badge"
        android:layout_width="wrap_content"
        android:layout_height="18dp"
        android:layout_gravity="top|end"
        android:layout_margin="2dp"
        android:minWidth="18dp"
        android:paddingStart="4dp"
        android:paddingEnd="4dp"
        android:background="@drawable/bg_glass_dock_rail"
        android:textColor="#FFFFFF"
        android:textSize="9sp"
        android:textStyle="bold"
        android:gravity="center"
        android:text="1"
        android:visibility="gone" />

</FrameLayout>
